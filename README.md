# WANDERLY Travel Booking Frontend

HTML + Tailwind CSS + JavaScript.

## Run
Open PowerShell in this folder:
python -m http.server 5500
Then visit http://localhost:5500

## Images
The image folders and filenames are already prepared. Replace the README.txt files with your generated image assets, keeping the exact filenames.
