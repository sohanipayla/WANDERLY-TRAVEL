document.addEventListener('DOMContentLoaded',()=>{const ids=JSON.parse(localStorage.getItem('wanderlyWishlist')||'[]'),all=[...destinations.map(d=>({...d,type:'Destination'})),...hotels.map(h=>({...h,type:'Hotel',description:h.location}))],items=all.filter(x=>ids.includes(x.id));document.querySelector('#wishlistGrid').innerHTML=items.map(x=>`<article class="bg-white rounded-3xl overflow-hidden shadow-sm"><img src="${x.image}" class="w-full h-56 object-cover"><div class="p-5"><p class="text-sm text-slate-500">${x.type}</p><h3 class="text-xl font-bold">${x.name}</h3><p>${x.description||''}</p></div></article>`).join('')||'<p class="text-slate-500">Your wishlist is empty.</p>';});