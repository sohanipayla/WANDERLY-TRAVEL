document.addEventListener('DOMContentLoaded',()=>{const grid=document.querySelector('#hotelGrid'),s=document.querySelector('#hotelSearch'),sort=document.querySelector('#hotelSort');function render(){let list=hotels.filter(h=>h.name.toLowerCase().includes(s.value.toLowerCase())||h.location.toLowerCase().includes(s.value.toLowerCase()));if(sort.value==='price')list.sort((a,b)=>a.price-b.price);if(sort.value==='rating')list.sort((a,b)=>b.rating-a.rating);grid.innerHTML=list.map(h=>`<article class="bg-white rounded-3xl overflow-hidden shadow-sm card-hover"><img src="${h.image}" class="w-full h-60 object-cover" alt="${h.name}"><div class="p-5"><div class="flex justify-between"><span class="text-amber-600">★ ${h.rating}</span><b>$${h.price}/night</b></div><h3 class="text-2xl font-bold mt-2">${h.name}</h3><p class="text-slate-500">${h.location}</p><a href="hotel-details.html?id=${h.id}" class="block text-center mt-5 bg-slate-900 text-white py-3 rounded-xl">View Hotel</a></div></article>`).join('')}s.addEventListener('input',render);sort.addEventListener('change',render);render();});